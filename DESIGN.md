# personal-wiki 设计方案

个人人际关系记忆与人格分析系统 —— 以"文件即数据库、opencode 即 Agent、静态页即看板"为原则的极简落地方案。

---

## 1. 项目定位与目标

### 1.1 解决什么问题
- 记住"谁在什么时候说了什么"，以及我对这个人的评价；
- 让 AI（opencode）基于这些一手记录，客观分析人物性格，并给出可执行的应对策略；
- 用一个可视化的看板，一眼看清所有人物：各自性格如何、最近动态如何、我怎么跟他打交道。

### 1.2 设计三原则
| 原则 | 含义 |
|---|---|
| **极简** | 不用数据库、不用向量库、不部署服务，全部落地为文本文件 |
| **可控** | 所有输入、所有分析结论都可读、可编辑、可回滚 |
| **可溯源** | 每条分析结论都能追到支撑它的原始记忆 ID |

### 1.3 角色分工
| 层 | 承担者 | 职责 |
|---|---|---|
| 输入层 | 用户本人 + HTML 表单 | 规范化录入，生成记忆 JSON 文件 |
| Agent 层 | opencode | 读记忆文件，产出人物档案与分析结论 |
| 展示层 | 生成式静态看板 | 数据可视化，浏览人物画像 |

---

## 2. 总体架构

```
┌─────────────┐      ┌────────────────────┐
│  输入层       │      │  摄入层 ingest       │
│  录入表单      │─────▶│  校验 / 决定落盘位置  │
│  → 生成JSON    │  下载 │  更新 register      │
│  （浏览器，不写盘）│      │  (pipeline/ingest.py │
└─────────────┘      │   或 opencode 执行)   │
                    └─────────┬────────────┘
                              ▼
┌─────────────┐      ┌──────────────────┐
│ data/       │─────▶│ Agent 层 (opencode)│
│ 记忆文件 JSON│      │ 读全部记忆 → 分析   │
│ 人物档案 MD  │◀─────│ → 更新档案         │
└──────┬──────┘      └──────────────────┘
       │
       ▼
┌─────────────┐
│ 展示层       │  build 生成静态看板
│ 静态 HTML     │  （双击可开，离线可用）
└─────────────┘
```

**关键约束：**
- 数据永远以 `data/` 下的文本文件为唯一事实源（single source of truth）；
- **浏览器不写盘、不出网**：表单只负责"生成 + 下载"记忆 JSON，一切落盘、校验、索引更新由 `pipeline/ingest.py`（或交给 opencode 执行）完成（见 5.5）；
- Agent 只读文件、只写文件，不维护任何内存态；
- `register.json` 是**生成物**，由 ingest/build 脚本从记忆目录重算，永不手工编辑；
- 看板是"生成物"，随时可由 `data/` 重新生成，永不手工编辑。

---

## 3. 目录结构

```
personal-wiki/
├── README.md                       # 项目说明（入口文档）
├── DESIGN.md                       # 本设计文档
├── .gitignore                      # git 忽略规则
│
├── data/                           # ★ 数据唯一事实源（进入 git）
│   ├── aliases.md                  # 人名归一化映射表
│   ├── people/                     # 人物档案（Agent 生成/更新）
│   │   └── zhangsan.md
│   ├── memories/                   # 原始记忆（摄入层落盘）
│   │   └── 2026/
│   │       └── 08/
│   │           └── 20260813-H1430-zhangsan.json
│   ├── private/                    # ★ 私密记忆（sensitive=true，gitignore 永不入库）
│   └── meta/
│       ├── register.json           # 记忆登记索引（生成物，见 4.4）
│       └── pseudonyms.md           # 假名映射表（仅展示层使用）
│
├── input/                          # 输入层（仅生成器，不写盘）
│   ├── index.html                  # 专业录入表单（浏览器打开 → 生成并下载 JSON）
│   ├── form-schema.json            # 表单字段的 schema（校验/生成用）
│   └── README.md                   # 录入使用说明
│
├── dashboard/                      # 展示层源码与生成物
│   ├── templates/                  # 页面模板
│   │   ├── index.html.j2           # 人物列表页模板
│   │   └── person.html.j2          # 人物详情页模板
│   ├── assets/                     # 静态资源（本地化，离线可用）
│   │   ├── echarts.min.js          # 可视化库（雷达图等）
│   │   ├── base.css
│   │   └── app.js
│   └── output/                     # ★ 生成出的看板（gitignore）
│       └── index.html
│
├── pipeline/                       # Agent 工作区 + 摄入脚本
│   ├── analyze.md                  # 人物分析指令模板（opencode 任务书）
│   ├── review.md                   # 定期回顾指令模板
│   └── helpers/
│       ├── ingest.py               # 摄入脚本：校验 → 落盘 → 重算 register.json（见 5.5）
│       └── build.py                # 看板构建脚本（读 data → 生成 output）
│
└── docs/
    ├── 使用手册.md
    └── 分析维度说明.md             # 性格维度的定义与打分规则
```

---

## 4. 数据模型

### 4.1 记忆文件（raw memory）—— JSON

文件名规范：`YYYYMMDD-HHmm-<人物规范化ID>.json`，如 `20260813-H1430-zhangsan.json`

```json
{
  "id": "20260813-H1430-zhangsan",
  "schema_version": 1,
  "created_at": "2026-08-13T14:30:00+08:00",
  "person": {
    "name": "张三",                  // 真名（数据层真实记录）
    "ref": "zhangsan",               // 规范化 ID，来自 aliases.md
    "relation": "同事-产品线总监"
  },
  "scene": {
    "type": "一对一会议",           // 枚举：一对一会议/多人会议/饭局/电话/微信/邮件/公共场合/其他
    "location": "上海-会议室A",
    "context": "季度项目复盘会"
  },
  "record_type": "对话",           // 枚举：对话/事件/评价/承诺/冲突/求助/成果
  "subject": {
    "situation": "项目进度滞后一周，双方在讨论排期。",
    "quote": "他说：'这个需求目前做不了，但我们可以先用变通方案绕过去。'",
    "action": "当场用白板画了两条替代路径，主动提出让后端评估工作量。",
    "result": "初步达成一致，约定周五给评估结论。"
  },
  "observation": {
    "demeanor": {"direct": 4, "rational": 3, "proactive": 2},     // 1-5 行为标尺
    "emotion": "calm",                                            // calm/positive/tense/excited/frustrated/angry
    "notes": "情绪稳定，但被问到底层限制时略有防御。"
  },
  "evaluation": {                                                 // 我的主观评价
    "strengths": "务实、不画饼、善于找替代路径",
    "weaknesses": "缺乏提前预警的意识，习惯后置暴露问题",
    "summary": "技术上可依赖，进度管理上需要替他设里程碑。"
  },
  "tags": ["技术决策", "沟通风格", "项目复盘"],
  "follow_up": "周五前确认后端评估结论",
  "sensitive": false,
  "related_person_refs": ["lisi"]
}
```

### 4.2 人名归一化表 aliases.md

> 防止"张三""张三 "“张-三”被当成两个人。Agent 在录入与分析前必须先读此表。

```markdown
# 人名归一化映射
张三  →  zhangsan   (关系: 同事-产品线总监)
张三(产品) → zhangsan
Lily → lily
```

冲突规则：新名字首次出现时，交给 Agent 判断是否与已有别名表匹配，匹配则并入、否则新建条目。

### 4.3 人物档案 person.md —— 由 Agent 分析生成

```markdown
# 人物档案：张三

## 基本信息
- ref: zhangsan   relation: 同事-产品线总监   首次记录: 2026-05-02
- 真名: 张三    假名: 老张（仅展示层使用）
- 记忆数: 12 条    最近互动: 2026-08-13

## 性格画像（updated: 2026-08-13, 证据索引: [m1][m4][m7]）
- 大五人格: 开放性 3.2 / 尽责性 4.1 / 外向性 2.5 / 宜人性 3.6 / 神经质 2.8
- 沟通风格: 直接、结论先行、少寒暄、用数据和案例推动
- 决策模式: 风险规避型，倾向在已有路径上优化而非冒险
- 冲突处理: 回避正面冲突，用"技术不可行"作为缓冲
- 情绪特点: 表面稳定，压力大时易出现轻微防御性反驳

## 应对策略（updated: 2026-08-13）
- 沟通: 先抛结论与数据，避免情绪铺垫；当面提问比邮件更高效
- 合作: 主动替他设定里程碑与预警点，他会感激但不会主动要求
- 汇报: 预先准备替代方案再提出风险，否则会触发防御
- 边界: 不与其在技术方案上正面争辩，用实验数据说话

## 行为变化趋势
- 2026-05 前后: 汇报风格从被动答复 → 6月起开始主动同步风险（转变标注: 可能与晋升压力有关）

## 待跟进
- 周五确认后端评估结论

> 证据引用规则：每条结论后标 `[m序号]`（m 由 register.json 分配，见 4.4）；旧结论与新证据冲突时，以近 3 个月记忆为准，并在此标注"行为模式发生转变"。
```

### 4.4 登记索引 register.json（生成物）

汇总全部记忆的最短信息，供看板与 Agent 快速扫描。**由 `ingest.py` / `build.py` 依据 `memories/` 目录自动重算，任何人（含 Agent）不手工编辑**，避免与记忆文件漂移。

每条记忆分配唯一 `m 序号`（按时间先后递增）——**档案与结论里的证据引用统一使用该 m 序号**（见 4.3 的 `[m1]`）。

```json
{
  "m": 42,
  "total": 42,
  "people": [
    {"ref": "zhangsan", "name": "张三", "memory_count": 12, "last_updated": "2026-08-13"}
  ],
  "memories": [
    {"m": "m42", "id": "20260813-H1430-zhangsan", "ref": "zhangsan",
     "date": "2026-08-13", "time": "14:30", "record_type": "对话", "sensitive": false}
  ]
}
```

- `sensitive=true` 的记忆落盘在 `data/private/`，**不进入** register.json、不参与分析与看板；
- 若 m 序号与记忆文件不一致（重算覆盖），档案中按新序号引用。

### 4.5 假名映射表 pseudonyms.md（展示层脱敏）

> 真名只存在于 `data/` 与档案 md 中；展示层一律用假名。看板即使外发，也绝不暴露真名。

```markdown
# 假名映射（仅展示层使用，ref → 假名）
zhangsan → 老张
lisi     → 李工
```

- 新建人物时在表单中登记假名；未填则自动生成占位假名（如「代号A」）并提示补全；
- 假名一经确定保持稳定，不随记忆内容变动；
- 构建看板时 `build.py` 依据此表做脱敏替换（详见 7.5）。

---

## 5. 输入层设计（规范化、专业化录入）

### 5.1 表单（input/index.html）

单页表单，纯 HTML + JS + 本地存储，浏览器双击即用，无服务器。分四区块：

| 区块 | 字段 | 说明 |
|---|---|---|
| **① 基础信息** | 人物（手动输入，localStorage 记忆最近人名便于复用）；**假名**（新建人物时登记，展示层用）；日期时间；场景类型（枚举下拉）；场景说明；记录类型 | 人物不读取本地文件（浏览器无写盘/读盘能力）；新名字的归一化由摄入层 / Agent 判断（见 5.5） |
| **② 事件主体** | 情境 S / 原话引用 Q / 行动 A / 结果 R | STAR 结构，客观还原，原话支持粘贴聊天记录 |
| **③ 人物观察** | 行为标尺（直接-委婉、理性-感性、主动-被动等 1-5 分）；情绪状态（单选）；补充观察 | 客观维度强制打分，主观notes可选 |
| **④ 评价与后续** | 优点 / 弱点 / 总结（三段式）；益处与风险；标签（已有标签选择+新增）；待跟进事项；敏感度开关 | 评价必须写"依据"，即指出是观察还是推断 |

### 5.2 校验规则（form-schema.json 驱动）
- 必填：人物、时间、场景、行动/原话（至少其一）、总结
- 原话与行动禁止空白，评价不得为空泛形容词（检测长度与关键词）
- 标签去重、归一化（同义词合并——表单内做轻量合并，跨人名/全量归一化由摄入层校验）
- 敏感度（sensitive = true）开关：置为私密的记忆，摄入层落盘到 `data/private/` 且被 gitignore

### 5.3 录入产物（表单 = 生成器，不写盘）
提交后表单**在浏览器内完成 schema 校验并生成记忆 JSON 文件，通过浏览器"下载"保存**：
- `sensitive=false` → 建议按 4.1 规范命名 `YYYYMMDD-HHmm-<ref>.json`，下载到本地待摄入；
- `sensitive=true` → 文件名加 `-PRIVATE` 后缀（如 `20260813-H1430-zhangsan-PRIVATE.json`），永远不得进入 git。

### 5.4 录入门槛（专业性的保证）
> 宁可少录，不可错录。所有主观评价必须绑定"我的观察依据"或显式声明为"我的推断"，禁止把猜测写成事实。

### 5.5 摄入流程（ingest.py / opencode 执行）
把下载的记忆 JSON 交给 `python pipeline/helpers/ingest.py <file>`（或作为交付物丢给 opencode），流程为：
1. **校验**：按 4.1 schema 重新校验，指出必填/格式问题；
2. **人名归一化**：查 `aliases.md`，已有 ref 则归并；新名字由 Agent 判断并入或新建条目；
3. **落盘**：`sensitive=true` → `data/private/YYYY/MM/`（不更新 register）；否则 → `data/memories/YYYY/MM/`；
4. **重算 register.json**：由脚本增量重算（新记忆分配 m 序号，见 4.4），不手工改；
5. **输出提交提示**：生成 `mem:` 提交信息建议，等待用户确认后执行 git commit。

> 注：`ingest.py` 与用户之间也可由 opencode 充当"对话式接收器"——用户直接粘贴表单下载的 JSON，opencode 执行上述 1-5 步。

---

## 6. Agent 分析层设计（opencode）

### 6.1 触发方式
4 种，全部由用户在 opencode 对话中发起：
- `录入 ...`：接收用户粘贴/指名的下载记忆 JSON，执行 5.5 摄入流程；
- `分析张三`：对单个人物运行完整分析；
- `分析全部`：遍历所有有新增记忆的人物并序贯分析；
- `回顾`：对某个历史结论做定期复核（review.md）。

### 6.2 分析流水线（analyze.md 任务书核心流程）
1. **读别名表** → 锁定 ref，确认人物唯一；
2. **读档案**（若存在）→ 作为旧结论上下文；
3. **读全部记忆** → 按时间排序，作为新证据；
4. **校验**：过滤"推断当事实"的记录，识别与旧结论的矛盾点；
5. **归纳**（见 6.3 维度）→ 产出性格画像、应对策略、变化趋势；
6. **写档**：更新 `people/<ref>.md`，每条结论标注 `[m序号]` 证据（m 来自 register.json，见 4.4）；
7. **索引**：register.json 是生成物，Agent 不手工改；档案更新后重跑 `ingest.py`（或 build 时一并重算）即可。

### 6.3 分析维度（定义见 docs/分析维度说明.md）
- 大五人格（开放性/尽责性/外向性/宜人性/神经质，1-5 分）
- 沟通风格（直接度 / 信息密度 / 寒暄偏好 / 修辞习惯）
- 决策模式（风险偏好 / 信息依赖 / 决策速度）
- 冲突处理（正面 / 回避 / 转移）
- 情绪特点（稳定性 / 表达度 / 压力反应）
- 合作价值（可依赖度 / 需补位点）

### 6.4 结论规范
- **事实与推断分离**：结论分"可确认事实（有证据 id 支撑）"与"AI 推断（标注 🧊 推断、置信度）"；
- **转变标注**：若近 3 个月行为与旧结论冲突，不删除旧结论，追加时间轴变化标注；
- **克制**：单次分析=1 条总结句 + 3-5 个关键标签 + 每条策略≤1 行，防止过度推断。

---

## 7. 展示层设计（可视化优化）

### 7.1 生成方式
`pipeline/helpers/build.py` 读 `data/`（含 register.json、pseudonyms.md）→ 套用 `dashboard/templates/*.j2` → 输出静态 `dashboard/output/`。输出内嵌 JSON 数据，**不依赖后端**，直接用浏览器打开 `index.html` 即可，也可 `python -m http.server` 获得完整体验。

### 7.2 人物列表页（index.html）
- **顶部 KPI 条**：人物总数 / 记忆总数 / 本月新增 / 待跟进数；
- **人物卡片墙**：
  - 头像（姓氏首字 + 主题色自动生成）
  - 姓名 + 关系
  - 3 枚性格标签（Agent 生成的 top 标签）
  - 情绪指数（近期记忆情绪的平均色块）
  - 大五人格迷你雷达（选中后悬浮放大）
  - 最近互动日期 + 记忆条数
- **排序/筛选**：按最近互动 / 记忆数 / 标签 / 关系类型；即时搜索框。
- **底部**：最近 N 条全局记忆时间线（轻量回放）。

### 7.3 人物详情页（person.html）
- 左侧：完整档案渲染（基本信息 / 性格画像 / 应对策略 / 变化趋势 / 待跟进）；
- 右侧：
  - 大五人格大雷达图（ECharts）；
  - 情绪-时间折线（历次互动的情绪变化轨迹）；
  - 记忆时间线（按时间倒序，点击展开原始记录、直接看到"原话引用"）；
- 明暗双主题，标签着色，全程可离线。

### 7.4 依赖约束（"零依赖"的准确边界）
- 构建脚本 `ingest.py` / `build.py`：**Python 3 + Jinja2**（仅构建期依赖，运行期产物零依赖）；
- ECharts 5：本地 vendor 一份（`dashboard/assets/echarts.min.js`），**首次安装时手动下载**后入库，离线可用；
- 其余全部原生 HTML/CSS/JS，避免框架版本漂移。

### 7.5 假名机制（展示脱敏）
- **真名存放**：`data/` 下的记忆 JSON 与人物档案 md 一律记录真名（数据层永远可信、可溯源）；
- **展示替换**：`build.py` 构建看板时，依据 `data/meta/pseudonyms.md` 将页面中出现的真名替换为假名：
  - 人物卡片姓名、详情页标题、基本信息 → 假名；
  - 档案正文、记忆时间线中的原话与场景描述 → 同时替换该人物及其关联人物（`related_person_refs`）的真名；
  - 无法可靠替换的残余内容（如原话中的姓、公司名），给出**"未脱敏"标记**而非静默放过，提示用户自行决定是否在录入时改写。
- **职责边界**：假名机制只做"人名"级脱敏；更深的隐私（具体项目、数据）由录入者在表单「敏感度」与场景描述中自行把控；
- 看板页面上固定显示「*展示内容为假名，真名仅存于 data/ 目录*」提示。

---

## 8. Git 管理策略

### 8.1 仓库初始化
- 根目录 `git init -b main`；
- 初始提交：README / DESIGN / .gitignore / 目录骨架 / 表单与 schema。
- **前置环境**：Python 3 + Jinja2（`pip install jinja2`）；ECharts 首次下载到 `dashboard/assets/`（见 7.4）。README 写明以上初始化步骤。

### 8.2 .gitignore 关键项
```
dashboard/output/          # 看板为生成物，不入库
data/private/              # 敏感/私密记忆（sensitive=true 的落盘位置）
*.log
.DS_Store
Thumbs.db
```
> 私密记忆（sensitive=true）由摄入流程直接落盘到 `data/private/` 且永不入库（见 5.5）；如需真正保密，目录可放在仓库外并在 aliases 中标注系统外管理。
>
> **真名与隐私警示**：`data/` 存真名并纳入 git 管理（可追溯），意味着**仓库本身包含全部真实姓名与一手记录**。因此——**仓库仅限本机私有，严禁推到公开远端或共享给他人**；commit message 一律用 ref/假名（见 8.3），`dashboard/output/` 已脱敏且不入库，git 历史中不出现看板产物。
>
> 若将来确有外发/半公开需求，采用「仓库外 data/ 真名 + 仓库内假名副本」方案（真名目录整体 gitignore，仓库内另存一份假名记忆供展示），见 P3 打磨项。

### 8.3 提交约定（由 opencode 在用户确认后执行）
| 前缀 | 场景 | 示例 |
|---|---|---|
| `docs:` | 改设计/文档 | `docs: 输入表单字段表更新` |
| `mem:` | 新增/修正记忆 | `mem: 录入 zhangsan 20260813 复盘记录` |
| `analysis:` | 档案/分析更新 | `analysis: 张三 画像更新（证据 m1,m4）` |
| `feat:` | 表单/看板/构建脚本改动 | `feat: 详情页新增情绪时间线` |

- 每次录入后一个 `mem:` 提交；每次分析后一个 `analysis:` 提交；
- 消息正文可附 人物 ref + 记忆日期/m序号，便于 `git log --oneline` 按人检索；
- **提交前必须 `git status` 核对，只加入预期文件，绝不把 `data/private/`、密钥带入；** 提交信息不得使用真名，一律用 ref 或假名（如 `analysis: zhangsan 画像更新`），避免在 commit message 中泄漏真实姓名。

### 8.4 回滚与审计
- 记忆误录：改/删对应 JSON → `mem:` 提交，历史可回溯；
- 分析偏离：`git diff` 查看档案变更，必要时 `git revert` 回退旧结论。

---

## 9. 实施路线

| 阶段 | 内容 | 可验收成果 |
|---|---|---|
| P0 骨架（本次） | git init、README、DESIGN、.gitignore、目录创建、表单 + schema + **ingest.py + 环境初始化（Python/Jinja2、ECharts vendor）**、1 条示例记忆 | `git log` 有初始提交；表单可生成并下载 JSON，`ingest.py` 可摄入示例 |
| P1 Agent 分析 | analyze.md 定稿；跑通"分析张三"，产出首份档案 | `people/zhangsan.md` 含画像+策略+证据引用（m 序号） |
| P2 看板 | templates + build.py，列表页 + 详情页跑通 | 双击打开 dashboard/output/index.html |
| P3 打磨 | 搜索/筛选/雷达交互/明暗主题/回顾机制 + review.md；**外发版「仓库外真名 + 仓库内假名副本」方案（可选）** | 体验层可日常使用；如启用，假名副本可安全外发 |
| P4 常态化 | 录入-分析-提交-看板 全流程，写进使用手册 | 形成个人使用闭环 |

---

## 10. 克制与边界（明确不做）
- 不引入数据库 / 向量库 / RAG（前期 数百条记忆 内文本搜索足够）；
- 不引入 Streamlit / FastAPI / 前端框架（静态页 + 原生 JS + 本地 ECharts 足够）；
- 不做实时聊天机器人、不接云同步；
- 不自动采集任何聊天记录 —— 一切录入均由人主动提交（隐私与可控的底线）；
- 不做跨人物冲突检测的复杂图谱（`related_person_refs` 仅作关联标记）。

---

*本设计遵循"文件即数据库、opencode 即 Agent、静态页即看板"三原则，后续迭代以 P0 → P4 顺序推进。*